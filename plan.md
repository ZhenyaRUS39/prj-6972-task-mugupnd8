# Plan: prj-6972-task-mugupnd8

**Project:** `PRJ-1694`  
**Task ID:** `task-mugupnd8`  
**Repo:** `prj-6972-task-mugupnd8`  

## Summary

Скрипт обработки изображений на PIL/Pillow: базовые операции (resize/crop/rotate/filter/format-convert), CLI-интерфейс, юнит-тесты и README. Разбито на 4 последовательных subtask.

## Subtasks

### 1. Создать структуру проекта и requirements

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать репозиторий: директории src/, tests/, examples/, input/, output/, .gitignore, requirements.txt (Pillow>=10.0.0, pytest>=7.0), README.md с описанием.
- **Test plan:** Файлы и директории существуют; requirements.txt валиден (pip install -r работает в venv); pytest --version отвечает.

### 2. Реализовать модуль обработки изображений

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** src/image_processor.py: класс ImageProcessor с методами resize, crop, rotate, flip, grayscale, blur, sharpen, change_format, batch_process. Все методы принимают Path/str, возвращают Path сохранённого файла, логируют ошибки.
- **Test plan:** from src.image_processor import ImageProcessor; ImageProcessor('input/test.jpg').resize((800,600)) возвращает Path существующего файла; обработка несуществующего файла бросает FileNotFoundError.
- **Dependencies:** `sub-1`

### 3. Реализовать CLI-интерфейс

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** src/cli.py на argparse: subcommands resize/crop/rotate/filter/convert/batch с параметрами (--input, --output, --width/--height, --angle, --filter, --format). Entry-point в src/__main__.py для запуска как python -m src.
- **Test plan:** python -m src resize --input test.jpg --output out.jpg --width 400 --height 300 создаёт файл; python -m src --help выводит список subcommands без traceback.
- **Dependencies:** `sub-2`

### 4. Написать юнит-тесты и example

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** tests/test_image_processor.py: pytest с фикстурой tmp_image (PIL.Image созданная в tmp_path). Покрыть все методы ImageProcessor (resize/crop/rotate/filter/format), batch_process, обработку ошибок. examples/example.py — демо-скрипт.
- **Test plan:** pytest tests/ -v — все тесты зелёные, coverage >= 80% для src/image_processor.py; example.py запускается и создаёт минимум 3 файла в output/.
- **Dependencies:** `sub-2`, `sub-3`


---
*Generated by Hermes Orchestrator*
